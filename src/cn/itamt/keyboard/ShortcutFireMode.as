package cn.itamt.keyboard {
	/**
	 * @author http://activeden.net/user/Flash2Load
	 */
	public class ShortcutFireMode {
		// 这种方式下的持续触发, 直到释放
		public static const INCESSANT : uint = 0;
		// 只在按下时触发一次
		public static const NORMAL : uint = 1;
	}
}
